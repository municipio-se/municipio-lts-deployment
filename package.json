{
  "name": "municipio-lts-boilerplate",
  "version": "2025.12.1",
  "scripts": {
    "build": "pnpm run build:mx",
    "build:mx": "cd web/app/mu-plugins/municipio-extended && pnpm i && pnpm run build",
    "format": "bunx --bun prettier . --write --ignore-unknown",
    "prepare": "husky ./.husky"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.22.4",
    "husky": "^9.1.7",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.6.14"
  }
}
